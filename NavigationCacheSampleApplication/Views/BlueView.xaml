<UserControl x:Class="NavigationCacheSampleApplication.Views.BlueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NavigationCacheSampleApplication.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="Blue">
    <Grid>
        <StackPanel Margin="100">
            <Label Content="{Binding Message}" FontSize="32"/>
            <Button Command="{Binding GetMessageCommand}">Get Message</Button>
            <Button Command="{Binding GotoRedCommand}">Goto red</Button>
        </StackPanel>
    </Grid>
</UserControl>
