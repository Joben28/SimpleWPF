<UserControl x:Class="NavigationCacheSampleApplication.Views.RedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NavigationCacheSampleApplication.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="Red">
    <Grid>
        <StackPanel Margin="100">
            <Label>Cache Message</Label>
            <TextBox Text="{Binding CacheMessage}" />
            <Button Command="{Binding GotoBlueCommand}">Goto Blue</Button>
        </StackPanel>
    </Grid>
</UserControl>
